import 'package:flutter/material.dart';

class ProfitScreen extends StatelessWidget {
  const ProfitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Profit Screen', style: TextStyle(fontSize: 24)),
    );
  }
}